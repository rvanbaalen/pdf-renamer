# Ollama configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3
# You can customize the system prompt if needed
# OLLAMA_SYSTEM_PROMPT=

# PDF processing
# PDF_EXTRACTION_COMMAND=/opt/homebrew/bin/pdftotext -layout -q "${filePath}" -
# PDF_MAX_SIZE=10485760  # 10MB

# Filename template
# Use Handlebars syntax with variables from LLM JSON response
# Available variables:
# - date.yyyy, date.mm, date.dd, date.full
# - company.name
# - invoice.number
# - summary.oneline
FILENAME_TEMPLATE="{{date.yyyy}}-{{date.mm}}-{{date.dd}} - {{company.name}} - {{summary.oneline}}.pdf"
# FILENAME_FALLBACK={{date.full}} - Unnamed Invoice.pdf
# FILENAME_SANITIZE=true
